{
  "name": "@ai-hub/android-app",
  "version": "0.1.9",
  "private": true,
  "description": "AI Hub Android WebView APK builds",
  "scripts": {
    "build": "node ./scripts/release.mjs",
    "release": "node ./scripts/release.mjs --bump",
    "version:print": "node -p \"require('./package.json').version\"",
    "lint": "echo \"no lint\"",
    "check-types": "echo \"no types\"",
    "dev": "echo \"android-app has no dev server\""
  }
}
